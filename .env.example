## Example environment variables for Leave-Request-Management
## Copy this file to .env (or to leave-management-app.env / leave-management-backend.env locally)
## and fill in real values. DO NOT commit real secrets to the repository.

# Django configuration
SECRET_KEY=replace-with-secure-secret
DEBUG=False
DJANGO_SETTINGS_MODULE=leave_management.settings_production
ALLOWED_HOSTS=.example.com

# Database (Postgres example)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# CORS and frontend
CORS_ALLOWED_ORIGINS=https://your-frontend.example.com

# Optional flags used by management commands during first-run (dev only)
RUN_DASHBOARD_FIX_ONCE=0
RUN_FIX_PRODUCTION_DATA=0
SETUP_FRESH_DATABASE=0

# Seeded/demo user credentials (only for local/dev; remove in production)
AAKORFU_EMAIL=example@company.com
AAKORFU_PASSWORD=changeme
GSAFO_EMAIL=example@company.com
GSAFO_PASSWORD=changeme
JMANKOE_EMAIL=example@company.com
JMANKOE_PASSWORD=changeme
HRADMIN_EMAIL=example@company.com
HRADMIN_PASSWORD=changeme

# Example: JSON used by seeding command (keep commented out)
# SEED_USERS='[ { "username": "jmankoe", "first_name": "Ato", "last_name": "Mankoe", "email": "jmankoe@umbcapital.com", "role": "manager", "department": "IT", "password": "Atokwamena" } ]'
