# Django Framework
Django==5.0.7
djangorestframework==3.15.2

# Python 3.13 compatibility
setuptools

# MySQL Database - Pure Python implementation
PyMySQL==1.1.1

# Environment Management
python-decouple==3.8

# CORS Headers for API
django-cors-headers==4.3.1

# Authentication & Authorization
djangorestframework-simplejwt==5.3.0

# API Filtering
django-filter==25.1

# Date/Time handling
python-dateutil==2.9.0

# Development & Testing
pytest==8.2.2
pytest-django==4.8.0

# Production server
gunicorn==22.0.0

# Database URL parsing for production
dj-database-url==2.1.0

# PostgreSQL adapter for production
# Note: On Windows dev machines without PostgreSQL client tools (pg_config), installing psycopg2-binary may fail.
# Keep this pinned for production environments where a prebuilt wheel or pg_config is available.
# If local installs fail on Windows, consider commenting this line out locally or using WSL.
psycopg2-binary==2.9.9

# WhiteNoise for static files in production
whitenoise==6.6.0

# Additional utilities
pytz==2024.1